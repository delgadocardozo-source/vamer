# VAMER — sitio estático

Landing (`vamer.html`), centro de mando (`admin.html`), CMS en `js/cms.js` y textos en `data/site-config.json`.

**Publicar en GitHub Pages:** seguí los pasos en [GITHUB-PAGES.md](GITHUB-PAGES.md).

Entrada pública: `index.html` → redirige a `vamer.html`.
